
function setup() {
    createCanvas(400, 400);
    background(220);
}

// function draw() {
//     background(220);
// }

function keyPressed() {
    for (i = 0; 1 < numCircle, i++;) {
        circle(i, i, rectSize);
        fill(colour, colour, colour);
    }
}

function keyReleased() {
    fill 
}