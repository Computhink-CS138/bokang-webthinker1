let squareX = 0;
let squareY = 0;
let speed = 5;

function setup() {
    createCanvas(600, 400);
    background(220);

    textSize(40);
    textAlig
    text("Hello!", width / 2, height / 2); // (string, x pos, y pos);
}

function draw() {

}
